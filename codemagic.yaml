# Codemagic build config — builds the Capacitor iOS app on a cloud Mac and submits it to
# App Store Connect / TestFlight. This is the Windows-friendly equivalent of the EAS flow you
# used for the (Expo) Harfika game — Chrono Empire is Capacitor, so it uses Codemagic instead.
#
# ONE-TIME SETUP in the Codemagic web UI (codemagic.io) before the first build:
#  1. Connect this project's GitHub/GitLab repo.
#  2. Teams → Integrations → App Store Connect → add an API key (Apple Developer → Users and
#     Access → Integrations → App Store Connect API → generate a key). Name the integration
#     exactly:  Chrono Empire ASC   (or change APP_STORE_CONNECT_ISSUER below).
#  3. Code signing → iOS → let Codemagic manage signing automatically (it creates the
#     certificate + provisioning profile from your Apple account).
#  4. Make sure an app record exists in App Store Connect with bundle id com.chronoempire.game.

workflows:
  ios-release:
    name: Chrono Empire iOS Release
    max_build_duration: 60
    integrations:
      app_store_connect: Chrono Empire ASC   # must match the integration name you created
    environment:
      node: 20
      xcode: latest
      cocoapods: default
      # NO ios_signing / use-profiles — both kept failing for this generated-in-CI Capacitor
      # project ("No matching profiles" / "App requires a provisioning profile"). Instead Xcode
      # manages signing itself with the App Store Connect API key (-allowProvisioningUpdates):
      # it creates/downloads the cert + profile automatically at archive time.
      vars:
        BUNDLE_ID: "com.chronoempire.game"
        TEAM_ID: "63VUWUSJ8H"
        XCODE_WORKSPACE: "ios/App/App.xcworkspace"
        XCODE_SCHEME: "App"
    scripts:
      - name: Install JS dependencies
        script: npm ci
      - name: Build the web app
        script: npm run build
      - name: Add & sync the iOS platform
        script: |
          npx cap add ios || true
          npx cap sync ios
      - name: Generate app icon & splash
        script: npx @capacitor/assets generate --ios || true
      - name: Add required native Info.plist keys (fixes the launch crash)
        script: |
          PLIST=ios/App/App/Info.plist
          set_str() { plutil -replace "$1" -string "$2" "$PLIST" 2>/dev/null || plutil -insert "$1" -string "$2" "$PLIST"; }
          set_bool() { plutil -replace "$1" -bool "$2" "$PLIST" 2>/dev/null || plutil -insert "$1" -bool "$2" "$PLIST"; }
          # GADApplicationIdentifier is REQUIRED by the Google Mobile Ads SDK; without it the app
          # throws on launch and crashes immediately.
          set_str GADApplicationIdentifier "ca-app-pub-3323428505450637~8715528140"
          # ATT description — needed because the app requests tracking authorization for ads.
          set_str NSUserTrackingUsageDescription "We use your device's ad identifier to show you relevant rewarded ads."
          # export-compliance: only standard HTTPS, so exempt.
          set_bool ITSAppUsesNonExemptEncryption NO
          plutil -lint "$PLIST"
      - name: Create export options
        script: |
          printf '%s\n' \
            '<?xml version="1.0" encoding="UTF-8"?>' \
            '<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">' \
            '<plist version="1.0"><dict>' \
            '<key>method</key><string>app-store</string>' \
            '<key>teamID</key><string>63VUWUSJ8H</string>' \
            '<key>signingStyle</key><string>automatic</string>' \
            '<key>destination</key><string>export</string>' \
            '</dict></plist>' > /tmp/export.plist
      - name: Archive (Xcode-managed automatic signing via the API key)
        script: |
          # Codemagic already placed the .p8 on disk; $APP_STORE_CONNECT_PRIVATE_KEY holds an
          # "@file:<path>" reference to it. Strip the prefix to get the real path.
          KEY_PATH="${APP_STORE_CONNECT_PRIVATE_KEY#@file:}"
          xcodebuild -workspace "$XCODE_WORKSPACE" -scheme "$XCODE_SCHEME" \
            -configuration Release -archivePath "$CM_BUILD_DIR/build/App.xcarchive" \
            -allowProvisioningUpdates \
            -authenticationKeyPath "$KEY_PATH" \
            -authenticationKeyID "$APP_STORE_CONNECT_KEY_IDENTIFIER" \
            -authenticationKeyIssuerID "$APP_STORE_CONNECT_ISSUER_ID" \
            DEVELOPMENT_TEAM="$TEAM_ID" CODE_SIGN_STYLE=Automatic \
            archive
      - name: Export the .ipa
        script: |
          # Same cloud signing the (successful) Archive step used — needs an Admin-role API key
          # so export can access the cloud-managed distribution certificate.
          KEY_PATH="${APP_STORE_CONNECT_PRIVATE_KEY#@file:}"
          xcodebuild -exportArchive \
            -archivePath "$CM_BUILD_DIR/build/App.xcarchive" \
            -exportPath "$CM_BUILD_DIR/build/ipa" \
            -exportOptionsPlist /tmp/export.plist \
            -allowProvisioningUpdates \
            -authenticationKeyPath "$KEY_PATH" \
            -authenticationKeyID "$APP_STORE_CONNECT_KEY_IDENTIFIER" \
            -authenticationKeyIssuerID "$APP_STORE_CONNECT_ISSUER_ID"
    artifacts:
      - build/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true      # goes to TestFlight first (safe)
        # submit_to_app_store: false    # flip to true when you're ready for the public release
